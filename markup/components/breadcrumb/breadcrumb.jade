mixin breadcrumb(type)
	-var statica = ['Главная','МРТ шейного отдела']
	-var filter = ['Главная','МРТ шейного отдела','Выбор клиники']
	div.breadcrumb
		ul.breadcrumb__list.clearfix
			if type ==="statica"
				each item in statica
					li.breadcrumb__item
						a.breadcrumb__link(href="")=item
			else
				each item in filter
					li.breadcrumb__item
						a.breadcrumb__link(href="")=item